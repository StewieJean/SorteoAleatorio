import React from "react";
import FormularioExcluded from "../../components/ExcludedParticipants"

const ExcludedParticipants: React.FC = () => {

  return (
    <main className="relative min-h-screen flex justify-center items-center bg-gray-100">
      <FormularioExcluded />
    </main>
  );
};

export default ExcludedParticipants;
