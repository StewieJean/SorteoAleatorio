import React from "react";
import ShowParticipants from "../../components/RuletaSort"

const RuletaSort: React.FC = () => {

  return (
    <main className="relative min-h-screen flex justify-center items-center bg-gray-100 overflow-hidden">
      <ShowParticipants />
    </main>
  );
};

export default RuletaSort;
